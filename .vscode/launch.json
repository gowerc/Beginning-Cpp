{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch1",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/bin/${fileBasenameNoExtension}",
            "cwd": "${workspaceFolder}/",
            "preLaunchTask": "BuildMe",
        }
    ]
}